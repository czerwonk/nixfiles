require('mini.bracketed').setup {
  comment = { suffix = '', options = {} },
  file = { suffix = '', options = {} },
  undo = { suffix = '', options = {} },
  treesitter = { suffix = '', options = {} },
}
